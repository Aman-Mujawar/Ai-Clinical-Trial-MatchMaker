services:
  - type: web
    name: ai-clinical-trial-api
    env: python
    region: oregon
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    preDeployCommand: alembic upgrade head
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      # DB URL will be set manually in Render dashboard (not in repo)
      - key: APP_DB_URL
        value: ""
      - key: COMMON_DB_URL
        value: ""
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: ENVIRONMENT
        value: "production"
